language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-customelement' ]; then cd .. && eval "mv $currentfolder generator-customelement" && cd generator-customelement; fi

